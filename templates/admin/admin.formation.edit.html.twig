{% extends "baseadmin.html.twig" %}
{% block body %}
    <h3 class="text-center text-info mt-3">Édition de la formation "{{ formation.title }}"</h3>
    {{ form_start(formformation)}}
    {{ include ('_admin.formation.form.html.twig')}}
    {{ form_end(formformation)}}
{% endblock %}